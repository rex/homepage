.DS_Store
node_modules/
dist/
.astro/
.cache/
.env
.env.local
.env.*.local
.vercel/
.cloudflare/
*.log
npm-debug.log*
.vscode/
.idea/


# CDK
infra/cdk.out/
infra/.cdk.staging/

# Pierce-to-Claude scratch folder (résumé sources, logos, etc.)
# Useful artifacts get copied into public/ explicitly.
stuff-for-claude/
# Agent caches and logs (agentic-skeleton PR1)
.Codex/
.claude/logs/
.claude/settings.local.json
.claude/serena-initialized
.serena/
.aider.tags.cache.v*
.aider.chat.history.md
.aider.input.history

# Terraform agent artifacts
tfplan
tfplan.*
*.tfplan

# MCP/tool caches
.context7-cache/

# Harvest wiring-hint output (regenerated by `npm run harvest`)
scripts/gallery-suggestions.json

# Harvested screenshots are generated output (`npm run harvest`). Published/curated
# shots are committed explicitly with `git add -f`; this keeps un-published and
# PII-bearing captures (finances, auth screens, etc.) out of the public repo.
public/projects/

# Playwright storageState from `npm run harvest` (session cookies — never commit)
.harvest-auth/
